# AI SQL Agent 测试报告

测试时间: 2026-03-20

## 结论

- 当前版本已完成库存能力补充，并通过完整回归测试
- 本轮共执行 15 个核心测试点，结果 `15/15` 全部通过
- 修复了一个多维结果粒度校验过严的问题：`按渠道和系列` 场景不再被误判为重复结果

## 环境与数据基线

- 服务地址: `http://127.0.0.1:8502`
- 本地数据库: `sql_agent`
- 向量检索: 已启用，本地 embedding 模型为 `BAAI/bge-small-zh-v1.5`
- 当前知识文档数: `33`
- 库存基础数据:
  - `dim_warehouse = 14`
  - `fct_inventory_snapshot = 672`
  - `fct_inventory_flow = 20775`

## 覆盖范围

- 页面与接口:
  - `GET /`
  - `GET /api/models`
  - `GET /api/schema`
- 库存主题:
  - 库存总量 / 可用库存
  - 按仓库库存量 / 可用库存 / 缺货率
  - 按仓型库存金额 / 缺货率
  - 按仓库入库量 / 出库量
  - 按系列入库量 / 出库量
- 原有销售主题:
  - 各渠道 GMV / 退款金额 / 退款率
  - 各系列 GMV Top5
  - 各渠道复购率 / 复购人数
  - 各渠道履约率 / 平均履约时长
  - 各渠道 GMV 环比
- 大模型兜底:
  - 各渠道和系列 GMV
- 异常输入:
  - 非法 SQL 语法类型 `bad_engine`

## 测试结果

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `GET /` | 通过 | 页面可正常访问 |
| `GET /api/models` | 通过 | embedding 启用，文档数 `33` |
| `GET /api/schema` | 通过 | 包含 `fct_inventory_snapshot` 和 `fct_inventory_flow` |
| 库存总量和可用库存 | 通过 | `template` 路由 |
| 各仓库库存量/可用库存/缺货率 | 通过 | `14` 行 |
| 各仓型库存金额/缺货率 | 通过 | `2` 行 |
| 近14天各仓库入库量/出库量 | 通过 | `14` 行 |
| 近14天各系列入库量/出库量 | 通过 | `8` 行 |
| 近30天各渠道 GMV/退款金额/退款率 | 通过 | `4` 行 |
| 近30天各系列 GMV Top5 | 通过 | `5` 行 |
| 近30天各渠道复购率/复购人数 | 通过 | `4` 行 |
| 近30天各渠道履约率/平均履约时长 | 通过 | `4` 行 |
| 近30天各渠道 GMV 环比 | 通过 | `4` 行 |
| 近30天各渠道和系列 GMV | 通过 | `llm` 路由，`32` 行 |
| 非法 SQL 语法类型 | 通过 | 返回 `400`，错误提示正确 |

## 本轮修复

- 修复库存样例与口径文档中缺货率示例，统一为 `COUNT(DISTINCT product_id)`
- 修复多维结果粒度校验逻辑，改为校验最终维度组合唯一性，而不是单字段唯一性
- 复测后确认库存模板与原有销售模板无回归

## 风险说明

- 当前目录原本不是 Git 仓库，本次已准备按本地仓库方式提交
- 尚无远程仓库地址，无法直接执行远程 push
