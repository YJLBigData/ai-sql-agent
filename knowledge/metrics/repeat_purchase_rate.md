# 复购率口径

- 指标名: `repeat_purchase_rate`
- 中文口径: 复购率
- 默认公式:
  - 时间窗内同一分析维度下 `订单数 >= 2` 的用户数 / 支付用户数
- 过滤条件:
  - 默认限定 `pay_status = '已支付'`
- 说明:
  - 复购分析必须基于 `user_id`
  - 如果按渠道统计，用户在每个渠道内独立计算复购
  - 如果按系列统计，用户在每个系列内独立计算复购
